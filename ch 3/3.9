#include<iostream>
#include<string>
using namespace std;

class c5_bank{
	private:
	
	int account;
	public:
	c5_bank(int a){         //constructor
		account = a;
		cout<<"\t\tWELCOME TO C5 BANK\n\n ";
	}
	widraw(){           //withdraw ammount function
	
	cout<<"please enter ammount: ";
	    int a;
	    cin>>a;
		if(a>=1000 && a<=25000){
	    	account=account - a;
		cout<<"you have witdraw a ammount of " <<a<<" from your account\n";
		menu();
		}else {
			cout<<"\t\tERROR!"<<endl;
			cout<<"PLEASE ENTER VALUE BETWEEN RS.1000 TO RS.25000";
			menu();
		}
	}
	deposit(){          //deposit
	
	    cout<<"please enter ammount: ";
	    int a;
	    cin>>a;
	    if(a>=500 && a<=500000){
	    	account=account + a;
		cout<<"you have deposit a ammount of " <<a<<" to your account\n";
		menu();
		}else {
			cout<<"\t\tERROR!"<<endl;
			cout<<"PLEASE ENTER VALUE BETWEEN RS.500 TO RS.500000";
			menu();
		}
		
	}
	check_bal(){
		cout<<"your account balance is: "<<account<<endl;
	}
	menu(){
		int a;
		cout<<"\n1.CHECK BALANCE\n2.DEPOSIT CASH\n3.WIDRAW CASH";
		cin>>a;
		if(a==1){
			check_bal();
			menu();
		}else if(a==2){
			deposit();
			check_bal();
			menu();
		}else if(a==3){
			widraw();
			check_bal();
			menu();
		}else{
			cout<<"INVALID ENTRY!";
			cout<<"SELECT VALUE FROM MENUE";
			menu();
		}
		
	}
};


#include"c5_bank.cpp"
using namespace std;

main(){
	c5_bank bank(1000);
	bank.menu();
	}
